<Window x:Class="FfmpegVideoUtility.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:FfmpegVideoUtility.ViewModels"
        mc:Ignorable="d"
        Title="FFmpeg Video Utility"
        Width="1100"
        Height="700">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="Settings" Command="{Binding OpenSettingsCommand}" />
                <Separator />
                <MenuItem Header="Exit" Command="{Binding ExitCommand}" />
            </MenuItem>
        </Menu>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Content="{Binding QueueStatus}" />
            <StatusBarItem>
                <Button Content="View Logs" Command="{Binding OpenLogsCommand}" />
            </StatusBarItem>
        </StatusBar>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TabControl Grid.Column="0" Margin="12">
                <TabItem Header="Transcode">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="12">
                            <TextBlock Text="Input File" />
                            <DockPanel>
                                <TextBox Text="{Binding Transcode.InputFile, UpdateSourceTrigger=PropertyChanged}" Width="400"/>
                                <Button Content="Browse" Command="{Binding Transcode.BrowseInputCommand}" />
                            </DockPanel>
                            <TextBlock Text="Output File" />
                            <DockPanel>
                                <TextBox Text="{Binding Transcode.OutputFile, UpdateSourceTrigger=PropertyChanged}" Width="400"/>
                                <Button Content="Browse" Command="{Binding Transcode.BrowseOutputCommand}" />
                            </DockPanel>
                            <TextBlock Text="Preset" />
                            <ComboBox ItemsSource="{Binding Transcode.Presets}" SelectedItem="{Binding Transcode.SelectedPreset}" DisplayMemberPath="Name" Width="250" />
                            <TextBlock Text="Resolution" />
                            <ComboBox ItemsSource="{Binding Transcode.Resolutions}" SelectedItem="{Binding Transcode.SelectedResolution}" Width="200" />
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="CRF" VerticalAlignment="Center" />
                                <Slider Minimum="16" Maximum="28" Value="{Binding Transcode.Crf}" Width="200" Margin="8,0,0,0" />
                                <TextBlock Text="{Binding Transcode.Crf}" Width="50" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Preset" VerticalAlignment="Center" />
                                <ComboBox ItemsSource="{Binding Transcode.SpeedPresets}" SelectedItem="{Binding Transcode.SelectedSpeedPreset}" Width="150" />
                            </StackPanel>
                            <Button Content="Start Transcode" Command="{Binding Transcode.StartTranscodeCommand}" HorizontalAlignment="Left" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="Clip &amp; GIF">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="12">
                            <TextBlock Text="Input File" />
                            <DockPanel>
                                <TextBox Text="{Binding Clip.InputFile}" Width="400" />
                                <Button Content="Browse" Command="{Binding Clip.BrowseInputCommand}" />
                            </DockPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Start (hh:mm:ss)" />
                                <TextBox Text="{Binding Clip.StartTimestamp}" Width="120" />
                                <TextBlock Text="End" Margin="12,0,0,0" />
                                <TextBox Text="{Binding Clip.EndTimestamp}" Width="120" />
                            </StackPanel>
                            <TextBlock Text="Thumbnail Timestamp" />
                            <TextBox Text="{Binding Clip.ThumbnailTimestamp}" Width="120" />
                            <Button Content="Create Clip" Command="{Binding Clip.CreateClipCommand}" HorizontalAlignment="Left" />
                            <Button Content="Create GIF" Command="{Binding Clip.CreateGifCommand}" HorizontalAlignment="Left" />
                            <Button Content="Generate Thumbnail" Command="{Binding Clip.GenerateThumbnailCommand}" HorizontalAlignment="Left" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="Batch">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="12">
                            <TextBlock Text="Input Folder" />
                            <DockPanel>
                                <TextBox Text="{Binding Batch.InputFolder}" Width="400" />
                                <Button Content="Browse" Command="{Binding Batch.BrowseInputCommand}" />
                            </DockPanel>
                            <TextBlock Text="Output Folder" />
                            <DockPanel>
                                <TextBox Text="{Binding Batch.OutputFolder}" Width="400" />
                                <Button Content="Browse" Command="{Binding Batch.BrowseOutputCommand}" />
                            </DockPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Normalize Resolution" />
                                <ComboBox ItemsSource="{Binding Batch.Resolutions}" SelectedItem="{Binding Batch.SelectedResolution}" Width="200" />
                            </StackPanel>
                            <Button Content="Scan Folder" Command="{Binding Batch.ScanCommand}" HorizontalAlignment="Left" />
                            <Button Content="Start Batch" Command="{Binding Batch.StartBatchCommand}" HorizontalAlignment="Left" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
            <Border Grid.Column="1" Background="#111" Margin="12" CornerRadius="8">
                <DockPanel>
                    <TextBlock Text="Job Queue" Foreground="White" FontSize="16" FontWeight="Bold" Margin="8" DockPanel.Dock="Top" />
                    <ListView ItemsSource="{Binding Jobs}" Margin="8">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Job" Width="200" DisplayMemberBinding="{Binding Description}" />
                                <GridViewColumn Header="Status" Width="100" DisplayMemberBinding="{Binding Status}" />
                                <GridViewColumn Header="Progress" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ProgressBar Minimum="0" Maximum="100" Value="{Binding Progress, Mode=OneWay}" Height="18" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </DockPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>
